#!groovy
pipeline {
  agent none
  stages {
    stage('Which branch') {
      sh 'git branch'
    }
    //stage('Clone Repository') {
      //checkout scm
    //}

    //stage('Build') {
      //sh 'sudo docker run --rm -v .:/app composer/composer install'
    //}

    //stage('Tests') {
      //sh 'bin/phpunit'
    //}
  }
}
